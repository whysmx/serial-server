run:
  timeout: 10m
  tests: true
  modules-download-mode: readonly

linters:
  enable:
    - gofmt
    - govet
    - revive
    - staticcheck
    - gosimple
    - ineffassign
    - unused
    - errcheck
    - gosec
    - gocyclo
    - dupl
    - misspell
    - goconst
    - gocritic
    - prealloc
    - nilerr
    - whitespace

linters-settings:
  govet:
    enable-all: true
    disable:
      - shadow

  gocyclo:
    min-complexity: 15

  dupl:
    threshold: 100

  goconst:
    min-len: 2
    min-occurrences: 3

  misspell:
    locale: US

  revive:
    rules:
      - name: blank-imports
      - name: context-as-argument
      - name: context-keys-type
      - name: dot-imports
      - name: error-return
      - name: error-strings
      - name: error-naming
      - name: exported
      - name: if-return
      - name: increment-decrement
      - name: var-naming
      - name: var-declaration
      - name: package-comments
      - name: range
      - name: receiver-naming
      - name: time-naming
      - name: unexported-return
      - name: indent-error-flow
      - name: errorf
      - name: empty-block
      - name: superfluous-else
      - name: unused-parameter
      - name: unreachable-code
      - name: redefines-builtin-id

issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - gocyclo
        - errcheck
        - dupl
        - gosec
        - goconst

    - path: listener/
      text: "G101"  # Ignore potential credentials hardcoded in listener tests

    # Ignore fmt.Scanln/Scanf errors in main.go (user input scenarios)
    - path: cmd/serial-server/main.go
      linters:
        - errcheck
      text: "Error return value of.*fmt.Scan(ln|f)"

    # Ignore fmt.Sscanf errors in frp.go (user input scenarios)
    - path: frp/frp.go
      linters:
        - errcheck
      text: "Error return value of.*fmt.Sscanf"

    # Ignore connection write errors (best-effort logging)
    - path: listener/listener.go
      linters:
        - errcheck
      text: "Error return value of.*conn.Write"

    # Ignore SetReadDeadline errors (non-critical)
    - path: listener/listener.go
      linters:
        - errcheck
      text: "Error return value of.*conn.SetReadDeadline"

  max-issues-per-linter: 0
  max-same-issues: 0

output:
  formats:
    - format: colored-line-number
  print-issued-lines: true
  print-linter-name: true
